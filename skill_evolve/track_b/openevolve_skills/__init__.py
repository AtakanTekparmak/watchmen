"""Forked openevolve modules, adapted for folder-of-files evolution.

The canonical openevolve package at repo-root (``openevolve/``) is left
untouched; this sub-package only re-implements the pieces we had to
modify. See module docstrings for the specific deltas.
"""
